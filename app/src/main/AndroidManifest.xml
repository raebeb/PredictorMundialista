<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.magister.unab.predictormundialista">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CountryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OtherActivity" />
        <activity android:name=".grupoDos" />
        <activity android:name=".Porcentaje" />
        <activity android:name=".porcentaje_a" />
        <activity android:name=".porcentaje_b" />
        <activity
            android:name=".porcentaje_c"
            android:label="@string/title_activity_porcentaje_c"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".porcentaje_d" />
        <activity android:name=".porcentaje_e" />
        <activity android:name=".porcentaje_f" />
        <activity android:name=".porcentaje_g" />
        <activity android:name=".porcentaje_h"></activity>
    </application>

</manifest>